# Handle text files automatically based on core.autocrlf (default)
* text=auto

# Ensure specific file types ALWAYS use LF endings in the repo
# AND enforce LF endings on checkout, regardless of core.autocrlf
*.sh text eol=lf
Dockerfile text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.properties text eol=lf
# Add other file types as needed (e.g., .md, .json, .xml)
*.md text eol=lf
*.json text eol=lf
*.xml text eol=lf